<%= form_for(colleage) do |f| %>
  <% if colleage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(colleage.errors.count, "error") %> prohibited this colleage from being saved:</h2>

      <ul>
      <% colleage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :phonenumber %>
    <%= f.text_field :phonenumber %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
